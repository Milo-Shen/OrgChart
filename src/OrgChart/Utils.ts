// Import Classes, Interfaces, Types
import { CardNode } from "./OrgChart";

export function find_the_most_left(node: CardNode): number {
  let distance = 0;

  return distance;
}

export function find_the_most_right(node: CardNode): number {
  return 0;
}
